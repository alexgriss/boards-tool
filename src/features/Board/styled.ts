import { styled } from 'styled-components';

export const BoardWrapper = styled.div`
  display: flex;

  width: 100%;
  min-height: calc(100vh - 38px);

  padding: 16px;

  overflow-x: scroll;

  user-select: none;
`;
